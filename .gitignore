node_modules/
.env
.env.local
.tmp/
.npm-cache/
dist/
.next/
*.log

# Client chat exports / business docs — never belong in the repo
Richie.txt
Bergen Car web Revisions.docx
clientchat.md

# Old WordPress backup + DB export — huge binary files, DB credentials, never belongs in the repo
/blog/
/sql/
